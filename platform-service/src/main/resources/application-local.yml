server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3308/observability?useSSL=false&allowPublicKeyRetrieval=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
  rabbitmq:
    host: localhost
    port: 5673
  data:
    redis:
      host: localhost
      port: 6381
